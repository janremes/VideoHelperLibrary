    import XCTest
    @testable import VideoHelperLibrary

    final class VideoHelperLibraryTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(VideoHelperLibrary().text, "Hello, World!")
        }
    }
